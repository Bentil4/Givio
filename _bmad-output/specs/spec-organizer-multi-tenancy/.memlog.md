---
topic: Organizer Multi-Tenancy & Role Hierarchy
updated: 2026-09-23T11:04
---

- (direction) Create, interactive. Input: two already-finalized, richly-structured upstream BMad artifacts -- prd-givio-2026-09-23/prd.md (26 FRs, 6 UJs, Glossary, NFRs, Risk register, Open Questions) and architecture-givio-2026-07-25/ARCHITECTURE-SPINE.md (AD-1..AD-13, ER/paradigm diagrams, Structural Seed, Deferred). Both are pre-sorted, express-distill mode -- no elicitation, trust the authored separation per skill instruction
- (decision) Both PRD and Architecture Spine adopted as companions (not sources) -- their FR/AD-level granularity has real ongoing downstream value for epics/stories, so full absorption-with-no-remaining-value doesn't apply. No new spec-authored companion needed: PRD already carries the Glossary (multi-item catalog) and Risk register; Spine already carries the diagrams (paradigm flowchart, ER diagram) -- duplicating either into a fresh companion would just create drift risk, so SPEC.md's kernel cites both adopted companions by section instead of re-authoring their content
- (capability) CAP-1 Tenant Hierarchy & Per-Event Access -- intent: access resolves per-Event, never company-wide, and tenants are structurally invisible to each other. success: zero confirmed cross-tenant data exposure incidents in production (PRD SM-2); no code path grants Event access without a matching per-Event grant (PRD FR-1, FR-2; Spine AD-1/AD-2). Realizes PRD FR-1..FR-3
- (capability) CAP-2 Credentials & Identity -- intent: each tenant relationship authenticates independently, never one identity switching between companies. success: a person working two tenants holds two distinct, independently revocable credential sets (PRD FR-4, FR-5). Realizes PRD FR-4..FR-5
- (capability) CAP-3 Organizer Onboarding & Approval Gate -- intent: no event company can touch a real family's data before Admin vets and approves it. success: median approval turnaround <=3 business days (PRD SM-1), never achieved by skipping document/phone verification (SM-C1). Realizes PRD FR-6..FR-9
- (capability) CAP-4 Co-Organizer & Operator Lifecycle -- intent: tenant leadership can grow its own team under individually-attributable accounts, and revocation never erases who did what. success: 100% of donations retain correct recorder attribution after revocation (PRD SM-3); bypass attempts via co-Organizer/Operator addition are caught (SM-6). Realizes PRD FR-10..FR-13, FR-23, FR-24
- (capability) CAP-5 Trust & Fraud Prevention -- intent: the platform actively detects likely fraud (duplicate event registration) without punishing legitimate re-created events. success: duplicate-event flags reviewed within <=24h (PRD SM-7), false-positive rate tracked and never optimized away from (SM-C2). Realizes PRD FR-14..FR-15
- (capability) CAP-6 Family Experience & Dignity -- intent: a grieving family sees their event's full, real donation picture without being exposed to anyone outside their event, framed around dignity not generic privacy. success: 100% of non-personal-device family sessions auto-logout correctly on navigation-away (PRD SM-5). Realizes PRD FR-16..FR-18
- (capability) CAP-7 Admin Oversight, Support & Cross-Event Reporting -- intent: Admin and Super Organizer each get the platform-wide or tenant-wide visibility their role needs, with every Admin access itself logged. success: consolidated cross-event total reflects a new donation within 15 seconds (PRD FR-21; Spine AD-12 pins Admin read/write logging at query granularity). Realizes PRD FR-19..FR-22
- (capability) CAP-8 Platform Administration Hierarchy -- intent: exactly one Super Admin can manage Admin accounts themselves, so multiple platform Admins are never unchecked. success: unauthorized Admin-account-management attempts (non-Super-Admin caller) are rejected server-side with zero successes (PRD SM-8; Spine AD-11). Realizes PRD FR-25..FR-26
- (constraint) Per-event grant is the sole atomic unit of access -- no company-level blanket permission may ever be introduced, even for a tenant's own leadership (PRD S9, FR-1)
- (constraint) Tenant isolation enforced at the Appwrite permission/data layer via the AD-9 Function's tenant-match check, never UI-only (Spine AD-2)
- (constraint) No account that has ever recorded a donation, approved a Tenant, or taken an Admin action is hard-deleted -- only revoked/suspended via a status field (PRD FR-13/FR-26, Spine AD-1/AD-11)
- (constraint) The AD-9 Function is the sole writer of Labels, Memberships, Tenant approval state, and Admin/Super Admin account changes -- no client-side write path for any of these may ever be introduced (Spine AD-9)
- (constraint) Family-facing language and support scripts are framed around a grieving family's dignity, never generic data-privacy/compliance language (PRD S9 explicit framing requirement)
- (constraint) Money stays integer minor units, never floats (Spine AD-5, inherited, still binding across the tenant extension)
- (constraint) Duplicate-event detection may never block Event creation outright, only flag for Admin review (PRD FR-14/SM-C2)
- (constraint) Revocation/Tenant-suspension must immediately sweep and retract live Appwrite permissions, not wait for a separate assignedUserIds edit (Spine AD-2, tightened by 2026-09-23 reviewer gate)
- (note) Non-goals (from PRD S5, compressed): family-as-self-organizer without a professional event company; automated third-party company verification (v1 is manual only); full ticketing/support system (Contact Admin form only); offline-first handling / recorder-vs-viewer distinction / designing around event temporariness -- all explicitly excluded design constraints, not oversights; billing/monetization (the approval gate is fraud-prevention, not billing); in-app Super Admin succession (manual/out-of-band only, PRD S4.8)
- (note) Success signal (top-level, PRD SM-2/SM-3/SM-5): the three guarantees the platform cannot compromise on as it scales to many concurrent tenants -- zero confirmed cross-tenant data exposure incidents, 100% donation-attribution integrity survives any revocation, 100% of non-personal-device family sessions auto-logout correctly
- (constraint) Admin/Super Admin access logging is client-side and best-effort (accountability trail), deliberately not tamper-proof -- no future feature may treat this log as an unbypassable security boundary (Spine AD-12)
- (assumption) FR-5's one-account-per-tenant guarantee covers provisioning-time prevention only; detecting shared-credential *use* after an account exists is out of scope for v1 (PRD FR-5 assumption, carried forward)
- (assumption) Manual verification's Admin/ops-time cost (PRD FR-8) is acceptable at current expected Organizer signup volume; revisit if volume grows enough to make phone-verification-per-Organizer a bottleneck (PRD S9 assumption, carried forward)
- (question) Exact onboarding field list beyond company name/location/size/estimated-users/type (PRD OQ2)
- (question) Verification specifics: what document types count as valid, who conducts the phone verification call (PRD OQ3)
- (question) Numeric targets for SM-1 (approval turnaround) and Contact Admin form response-time expectation (PRD OQ4)
- (question) Does a billing/monetization model interact with the pending/approved Tenant state (PRD OQ5)
- (question) Exact identity-matching rules for the Operator-addition/IdentityFlags check -- which fields, what counts as a flaggable match vs. coincidental near-match (PRD OQ6, Spine AD-9/AD-13 Deferred, merged)
- (question) Super Admin succession mechanism if the single designated account is lost, compromised, or needs transfer (PRD OQ7, Spine AD-11 Deferred, merged)
- (question) Duplicate-event detection's exact matching algorithm -- which fields, fuzzy vs exact (Spine AD-13 Deferred)
- (question) Operations envelope (monitoring/alerting/backup) for the now-load-bearing AD-9 Function (Spine Deferred)
- (question) Deployment & environments (hosting, CI/CD, env promotion) -- long-standing, out of scope for both PRD and Spine, still needs an owner (Spine Deferred)
- (event) Self-validate pass 1 (coherence): PASS. All 8 CAP-Ns carry both intent and success; intents are WHAT not HOW; all 9 constraints genuinely bend a design decision (none decorative); 6 non-goals present; success signal is 3 concrete, quantified guarantees; CAP-1..CAP-8 IDs contiguous with no gaps/dupes; prose kept lean, no throat-clearing found on review
- (event) Self-validate pass 2 (preservation): PASS. Both source artifacts (PRD, Spine) adopted whole as companions rather than absorbed-and-discarded, so every FR-1..FR-26 and AD-1..AD-13 remains directly readable by downstream, not just summarized. Already-resolved PRD items (former OQ1 migration, former OQ5 Admin-access-model) correctly NOT carried forward as open questions -- captured instead as kernel Constraints. No wrapper-only content found to log as dropped
- (event) spec finalized
