---
scope: Givio Donation Management System - Appwrite data model, offline-first Dexie sync, RBAC, Angular module boundaries
purpose: Build substrate for implementing 26 planned screens against a consistent data/sync/auth contract
altitude: project (above epics)
updated: 2026-07-26T00:39
---

- (constraint) [ADOPTED] Three roles only: Admin, User/Operator (called 'organizer' in screen specs), Family Member (called 'member' in screen specs) — per PRD 4.2. Donors have no accounts/login (PRD 4.1, Low influence/external).
- (constraint) [ADOPTED] Operators are assigned to one or more specific events and must not read/write events they are not assigned to, enforced at Appwrite DB permission level not just UI (FR-SEC-002).
- (constraint) [ADOPTED] Family Members get read-only access scoped to their own single event only (PRD 4.2, FR-SEC-002).
- (constraint) [ADOPTED] Donor phone numbers restricted to Admin + assigned Operators only; excluded from Family Member API responses and receipt PDFs (FR-SEC-003).
- (constraint) [ADOPTED] Full immutable audit log required for every create/edit/delete: user id, action, timestamp, before/after values; Admin-only viewable (FR-SEC-004).
- (constraint) [ADOPTED] Sessions expire after 8h inactivity, refresh tokens rotate on use, concurrent multi-device sessions allowed, Admin can force-expire a user's sessions (FR-SEC-005).
- (constraint) [ADOPTED] Sync conflicts (same record edited by two offline users) must be detected and flagged for Admin review showing BOTH versions, with manual keep/merge resolution, logged (FR-OFF-005) — this is NOT last-write-wins; conflicting versions must be preserved somewhere until Admin resolves.
- (constraint) [ADOPTED] All donation entry/edit/receipt generation must work fully offline via IndexedDB; auto-sync on reconnect within 5s, background, toast on completion, retry on failure (FR-OFF-002/004).
- (constraint) [ADOPTED] No payment gateway; all amounts manually entered by operators. Currency defaults to GHS. Must support >=10 concurrent operators across different events (PRD 5.1/5.2).
- (constraint) [ADOPTED] Must ship as a PWA (no native builds v1) — @angular/service-worker already registered in app.config.ts (duplicated call, needs cleanup) but ngsw-config.json has no dataGroups yet, asset caching only.
- (constraint) [ADOPTED] Receipt generation offline requires an in-browser PDF lib (jsPDF/pdfmake, PRD 5.2/5.3) and report export requires an Excel lib (SheetJS/xlsx, PRD 5.2/5.3) — not yet in package.json, must be added.
- (constraint) [ADOPTED] Appwrite Cloud instance already provisioned (env: fra.cloud.appwrite.io, project 69c270d10029e7ed7f82); PRD 5.1 notes realtime support required from the Appwrite plan.
- (constraint) Brownfield: src/lib/appwrite.ts wraps only Client+Account, hardcodes endpoint/project instead of reading src/environments — only Account API used anywhere, no Databases/Permission/Role usage yet.
- (constraint) Brownfield: dexie dependency installed but zero lines of Dexie code anywhere in src/ — fully greenfield for offline layer.
- (constraint) Brownfield: auth/service/authservice.ts is an empty unused stub; role concept currently lives only as IUserPrefs.role ('admin'|'user'|'member') read ad-hoc inside login.ts with no guard, no central AuthService, no route protection at all.
- (constraint) Brownfield: routing is flat/eager (app.routes.ts, 4 routes), no lazy loading; admin page tree exists (dashboard+widgets), user page tree is an empty shell, no organizer/member tree exists yet.
- (constraint) Brownfield: shared component library (button/input/card/checkbox/filter-tags/preloader/progress/radio/select/tag/toggle) all follow signal input()/output() + OnPush, CVA pattern for form controls, SCSS + CSS custom properties + Angular Material theming (no Tailwind) — only Button/Input/Preloader exported from barrel today.
- (constraint) Brownfield: no NgRx, no RxJS Subject/store anywhere; only local component signal state exists — no cross-component state layer established, this is an open decision for the spine.
- (constraint) Brownfield: no domain models exist yet for Event/Donation/Donor/Organizer — fully greenfield.
- (event) brownfield code sweep + PRD/problem-statement/system-flow doc review complete
- (decision) AD: Global role (admin/operator/member) is stored ONLY as an Appwrite user Label, never account.prefs. Binds: auth, all permission rules. Prevents: client self-privilege-escalation via updatePrefs (verified: prefs are client-writable, labels are server/console-only per Appwrite docs). Rule: AuthStore reads role from account.labels; login.ts's IUserPrefs.role pattern is removed; Appwrite collection/document permissions use Role.label('admin') etc. where a global (not event-scoped) check is needed.
- (decision) AD: Event-scoped access (operator assigned to event, family member scoped to event) is modeled as one Appwrite Team per Event. Binds: Events, Donations, event-scoped collections. Prevents: a custom assignedIds+Function reimplementation of what Appwrite Teams already provides natively; prevents operators/family reading events they aren't a member of even via direct API (FR-SEC-002). Rule: creating an Event provisions a Team; assigning an operator/family member = adding them to that Team (family members added with a restricted in-team role, e.g. 'viewer'); Event/Donation/Report documents carry permissions Role.team(eventId) for operators and Role.team(eventId,['viewer']) read-only for family members.
- (decision) AD: Sync conflicts (FR-OFF-005) are stored server-side only, in an Appwrite DonationConflicts collection with Admin-only (Role.label('admin')) read/write permission. Binds: sync engine, donation edit flow. Prevents: building a second offline conflict-review UI / local conflicts table for a scenario Admin resolves online. Rule: when the sync engine detects a version mismatch on push, it writes the losing version into DonationConflicts (both versions referenced) instead of overwriting the server document, and surfaces it only in the Admin dashboard.
- (decision) AD: Module shape is role-first pages + shared domain layer. Binds: src/app structure. Prevents: Event/Donation/Sync/Reporting logic being duplicated three times across admin/organizer/member trees. Rule: src/app/feature keeps admin/organizer/member page trees (1:1 with the .claude/skills/plan screen specs, renaming 'user'->'organizer' and building out 'member'), but ALL Event/Donation/Donor models, Appwrite access, Dexie access, sync engine, and signal stores live in one shared domain layer (e.g. src/app/data or src/app/core) that every role tree injects — never re-implemented per role.
- (event) coaching elicitation complete: 4 load-bearing decisions confirmed (role labels, team-per-event, server-side conflicts, role-first+shared-domain module shape)
- (version) jsPDF 4.2.1 is current on npm registry (verified web, ~9.6M weekly downloads) -- safe to 'npm install jspdf' normally for offline receipt PDF generation.
- (version) SheetJS 'xlsx' package on the npm REGISTRY is outdated/unmaintained and flagged vulnerable -- SheetJS stopped publishing to npm years ago. Official current install is from their own CDN tarball (cdn.sheetjs.com/xlsx-<ver>/xlsx-<ver>.tgz), vendored into the repo, NOT 'npm install xlsx'. Must document this in package.json/README so a future contributor doesn't silently pull the stale npm package.
- (version) appwrite web SDK ^23.0.0 and dexie ^4.3.0 already pinned in package.json and are current major versions -- no change needed.
- (decision) AD: Layered architecture -- Presentation (feature/{admin,organizer,member} page trees + shared UI) -> Domain/State (signal stores: AuthStore, EventStore, DonationStore, SyncStore, ReportService) -> Data (repositories: one per aggregate, each wrapping Appwrite (remote) + Dexie (local) behind a single interface). Binds: all. Prevents: a page/component calling Appwrite or Dexie directly and bypassing the offline cache/outbox. Rule: only the Data layer may import 'appwrite' or 'dexie'; Presentation and Domain/State never import them directly.
- (decision) AD: Offline write path uses an Outbox pattern in Dexie. Binds: DonationRepository, EventRepository. Prevents: divergent 'when do we write to Appwrite' logic per screen. Rule: every mutation writes to the local Dexie table AND appends an outbox entry (localId, entityType, op, payload, status: pending|synced|conflict, retries); a SyncEngine (root service) drains the outbox strictly in append order when online, updates outbox status per result, and on a version mismatch writes to the Appwrite DonationConflicts collection per the conflict-storage AD rather than overwriting.
- (decision) AD: consolidate the existing src/lib/appwrite.ts (hardcoded endpoint, Account-only) into src/app/data/appwrite/ reading endpoint/project from src/environments (fixes the current environment/hardcode divergence found in the brownfield sweep); Databases/Teams clients added alongside the existing Account client.
- (decision) AD: money amounts are stored and transmitted as integer minor units (pesewas, GHS x100), never floats/decimals, to avoid rounding-error divergence between client display and stored totals. Binds: Donation entity, all report/export code.
- (decision) AD: route guards are functional CanActivateFn (Angular 21 style, consistent with .instructions.md house rules). Global role (admin/operator/member) checked via AuthStore reading account.labels; event-scoped routes (an operator/family member opening a specific event) additionally check the event's Appwrite teamId against the caller's team memberships fetched at login. Binds: app.routes.ts and all lazy-loaded child route configs.
- (decision) AD: feature routes are lazy-loaded via loadChildren/loadComponent per role tree (admin/organizer/member), replacing the current flat eager app.routes.ts -- already a standing house rule in .instructions.md ('Implement lazy loading for feature routes'), just not yet applied.
- (decision) [ADOPTED] Consistency conventions inherited from .instructions.md (already-standing project rule, not re-decided here): standalone components, input()/output() signal functions, ChangeDetectionStrategy.OnPush, computed() for derived state, native control flow (@if/@for/@switch), Reactive Forms, inject() over constructor injection, providedIn:'root' services, class/style bindings not ngClass/ngStyle.
- (event) remaining structural/seed decisions logged, ready to distill spine
- (event) reviewer gate ran: lint_spine.py clean; 3 parallel subagent reviews (tech-verify, adversarial, rubric) found real issues -- reconciled below.
- (constraint) [ADOPTED, from PRD 8/9 -- previously missed, must ratify not contradict] PRD already specifies: assignedUserIds:String[] on Events collection (not Teams) as the assignment mechanism; Event.accessCode (hashed) for Family Member no-login read-only access; donations.syncStatus enum ('synced'|'pending'|'conflict'|'failed', client-local); conflict detection = compare local baseline vs Appwrite $updatedAt; donations use soft-delete (isDeleted/deletedAt/deletedBy), never hard delete; audit_logs collection schema (entityType/entityId/action/performedBy/previousValues/newValues/timestamp) already defined; Appwrite Realtime subscribed per-event donations collection for live dashboard updates (not deferred -- PRD commits to it for v1); Appwrite Functions named in PRD only as optional v1.1 for large Excel export.
- (decision) REVISED AD-2: drop Appwrite Teams (contradicted PRD's own explicit assignedUserIds:String[] data model). New rule -- assignedUserIds on Event is the SOLE source of truth for operator assignment; Appwrite document permissions on Event/Donation are a MECHANICALLY DERIVED projection (Role.user(uid) for each id in assignedUserIds, plus Role.label('admin') always) recomputed whenever assignedUserIds changes -- never hand-edited separately, so there is exactly one source of truth. Family read-only access is derived from Event.accessCode, not team membership. This also fixes 'Admin cross-event visibility' for free (Role.label('admin') is always present regardless of assignment).
- (decision) REVISED AD-3: conflict detection is exactly PRD 8.3's mechanism -- each outbox update entry carries baseUpdatedAt (the server $updatedAt seen when the local edit started); on push, if the server document's current $updatedAt != baseUpdatedAt, it's a conflict: write both versions to a DonationConflicts collection (Role.label('admin') only), set the local record's syncStatus='conflict' (reusing PRD's own enum), never overwrite the server doc.
- (decision) REVISED AD-4: Outbox entries generate the entity's Appwrite $id client-side via ID.unique() at creation time (works fully offline, no network needed) so retries are idempotent upserts-by-id, not duplicate creates. Outbox schema pinned: {id, entityType, entityId, op, payload, baseUpdatedAt (update ops only), status, retries, createdAt}. Ordering is per-entity FIFO (one entity's queue never blocks another entity's sync), not global-blocking FIFO.
- (decision) NEW AD-8: receipt numbers are provisional-then-final (user-confirmed). Offline: client assigns a local provisional number (e.g. '{eventShortCode}-P{n}') shown on the receipt with a small 'provisional' marker. On sync, the server assigns the canonical sequential number via an atomic nextReceiptSeq counter field on the Event document; the donation's receiptNumber is updated to the canonical value post-sync. Nothing already handed to a donor needs reprinting -- only the stored/reported number changes.
- (decision) NEW AD-9: user Labels (AD-1) can only be set server-side, but the app is otherwise pure-client with no backend of its own -- so ONE small Appwrite Function is introduced as the sole writer of Labels, invoked by the Admin's authenticated client call from admin-settings.md's 'Change Role' action. This is a real (small) scope addition beyond the PRD's v1.0 architecture section, which only anticipated an Appwrite Function as an optional v1.1 item for Excel export -- flagged to the user, who should confirm/note it in the PRD.
- (decision) NEW AD-10 (user-confirmed, PRD-baseline scope): v1 share access is exactly PRD FR-AUTH-004 -- one hashed read-only accessCode per event for Family Members. The richer share-access.md screen (tiered view/add/full, revocable, expiring, multi-link delegation for helpers with no account) is explicitly Deferred to v1.1+ since it needs its own signed-token + Function-verification mechanism distinct from accessCode.
- (constraint) Reconciliation note: sync-status.md's 'Conflict resolution section' (Keep Local/Use Server/Manual Merge) must render ONLY when the viewing user has Role.label('admin') -- non-admin viewers of the shared sync-status screen never see conflict diffs, satisfying FR-OFF-005/PRD 10.2's 'Conflict Resolution: Admin only' screen-access row, since the screen file itself doesn't gate this.
- (constraint) Accepted risk (not fully closable): an operator/family member unassigned from an event keeps their already-synced local Dexie cache of that event's data until their next login, since offline-first means no remote wipe of local storage. Mitigation: AuthStore re-fetches assignedUserIds/accessCode validity on every login/session-refresh and SyncEngine prunes locally cached data for any event no longer in the caller's current access set.
- (event) critical/high reviewer findings reconciled into revised ADs 2/3/4 + new AD-8/9/10; ready to re-distill spine v2
- (event) spine v2 distilled with all critical/high findings reconciled; lint clean
- (event) spine finalized
- (event) shareable HTML doc published as Artifact (https://claude.ai/code/artifact/f3c9a4ca-ebbc-4a6d-88f3-a5e8bceb383e); spine run closed
