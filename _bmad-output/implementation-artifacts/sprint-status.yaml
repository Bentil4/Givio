# generated: 2026-07-26
# last_updated: 2026-07-26
# project: givio
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-07-26
last_updated: 2026-09-25-story-6-2-review
project: givio
project_key: NOKEY
tracking_system: file-system
story_location: '{project-root}/_bmad-output/implementation-artifacts'

development_status:
  epic-1: in-progress
  1-1-secure-login-role-guarded-dashboard-landing: done
  1-2-server-side-role-writer-appwrite-function: done
  1-3-admin-user-management-create-edit-deactivate: done
  1-4-session-expiry-security-hardening: done
  epic-1-retrospective: optional

  epic-2: in-progress
  # 2-1/2-3 verified implemented in code (2026-09-06): edit-event.ts +
  # event-data.service.ts cover status guards; admin-event-detail.ts has
  # assignOperators wired to the set-role-and-permissions Function
  # (event-assignment.js).
  # 2-2 correction (2026-09-08): this was marked "done" above on
  # 2026-09-06 with a claim that pause/resume/close were "working" —
  # that was inaccurate. admin-event-detail.ts's confirm() only ever had
  # a single stub covering all four actions ("This action isn't
  # available yet in this build"); Phase 5 (2026-09-08) wired
  # 'regenerate' for real (Story 2.4) but deliberately left
  # pause/resume/close untouched, since no event-status-lifecycle
  # service exists anywhere in the codebase — only read-only computed
  # guards (canPause/canResume/isClosed) derived from event.status.
  # Reopened to backlog; the read-only guards belong to 2-1/2-3, not
  # this story.
  # 2-1 reopened (2026-09-07): reported bug — Admin/Operator can't see
  # events after logging in on a different browser. Root cause:
  # EventDataService.listEvents() only ever read local Dexie, never
  # queried Appwrite. New AC added to Story 2.1 in epics.md; fix
  # implemented same day (paginated listRows pull, permission-scoped,
  # skips events with a pending outbox entry, falls back to local read
  # offline) with unit tests. Status is "review", not "done" — not yet
  # manually verified logging into two actual different browsers (no
  # browser-automation tool available this session).
  2-1-create-edit-events: review
  # 2-2 implemented 2026-09-09: new Function action setEventStatus (event-assignment.js,
  # admin-only via verifyAdminCaller) validates transitions against ALLOWED_TRANSITIONS
  # (active<->paused, either->closed, closed->active only — a closed event can't skip
  # straight to paused, it must be reopened first) before writing Event.status. Guarded the
  # existing rejectNonOperatorIds check to only run for the assignOperators action, since it
  # previously ran unconditionally and would have thrown on setEventStatus's payload (no
  # assignedUserIds field). EventDataService.setEventStatus follows the regenerateAccessCode
  # pattern (online-only, no outbox — the transition is server-validated, nothing meaningful
  # to queue offline) and writes an audit log entry (action: 'edit', previousValues/newValues
  # capturing the status change) the same way updateEvent() does, satisfying the AC's "status
  # change is logged". admin-event-detail.ts's confirm() stub is gone — pause/resume/close/
  # reopen all call EventService.setEventStatus with the right target status; added a
  # 'reopen' action + button (shown in place of pause/resume/close when isClosed()) since the
  # AC explicitly requires reopening a Closed event, which the pre-existing UI copy
  # ("cannot be reopened — only an export remains") directly contradicted — that copy (in both
  # the confirm dialog and the Danger Zone aside) has been corrected to describe what the
  # Function actually does: closing blocks new donations but leaves history and the family
  # code untouched, and can be reopened later.
  # Known gap (out of scope for this story, belongs to Story 3.1's mobile form factor):
  # mobile-entry.ts has no client-side canRecord/blocked guard or status-aware messaging like
  # donation-entry.ts does — a paused/closed submission from mobile-entry still gets rejected
  # correctly (DonationDataService.createDonation's status check + donation-recording.js's
  # server-side check both already exist and are unaffected by this story), just without the
  # friendlier pre-emptive UI message.
  # Deployed via the Appwrite MCP server same-day (same flow as Stories 2.4/3.5): pushed this
  # branch, VCS auto-built deployment 6aa1171fe09780fdb8fd in 19s, then explicitly activated it
  # (functions_update_function_deployment) since the Function's providerBranch is 'dev', not
  # this feature branch. Smoke-tested via a live execution: an unauthenticated setEventStatus
  # call returned 401 "Unauthenticated", confirming the new deployment is live and routing
  # through verifyAdminCaller before ever reaching the handler.
  # Manually browser-verified 2026-09-09: pause/resume/close/reopen all exercised end to end
  # against the live deployment. Status: done.
  2-2-event-status-lifecycle: done
  2-3-assign-operators-permission-derivation: done
  # 2-4 implemented 2026-09-08: new Function actions generateAccessCode
  # (Admin-only, plaintext code stored on Event.accessCode — deliberate
  # deviation from the architecture doc's "hashed" wording, since
  # admin-event-detail.ts's copyCode() needs to re-read the code anytime
  # and Admin+assigned-Operators already hold row read access) and
  # resolveAccessCode (the one genuinely public, unauthenticated action
  # in the whole Function — a Family Member has no account, AD-10).
  # Had to open the Function's own `execute` permission from
  # ["users"] to ["users", "any"] via the Appwrite MCP server — Family
  # has no session, so the platform itself was rejecting the call before
  # ever reaching our code. family-code.ts's peek()/submit() wired to
  # FamilyAccessService; admin-event-detail.ts's "regenerate" action
  # wired to EventService.regenerateAccessCode. Deployed via the Appwrite
  # MCP server (push branch, VCS auto-build, explicit activate — same
  # flow as Story 3.5); smoke-tested via live executions: resolveAccessCode
  # with a bogus code returned 404 "Code not recognised" (not a platform-
  # level rejection, confirming the public path truly runs), and
  # generateAccessCode with no auth returned 401, confirming the
  # execute:any platform change didn't loosen the Function's own
  # per-action admin check.
  # Manually browser-verified 2026-09-08: Admin generated a code from
  # admin-event-detail.ts ("Quansah Funeral"), the code and Copy button
  # both worked; a fresh code was then entered at /family and resolved
  # correctly (verified server-side too — the live event row's current
  # accessCode round-tripped through resolveAccessCode and matched what
  # the Admin UI displayed). Status: done.
  2-4-family-access-code-event-code-login: done
  epic-2-retrospective: optional

  epic-3: in-progress
  # 3-1 verified against its 6 ACs (2026-09-06): AC1/AC3/AC6 implemented;
  # AC2 (offline outbox) core pattern solid, FIFO dequeue correctly deferred
  # to Story 3.5; AC4 (phone visibility) correct server-side, family
  # consumer path (family-live.ts) is a separate story's stub. AC5
  # (online/offline indicator) implemented same day via new
  # ConnectivityService (navigator.onLine + online/offline listeners),
  # wired into donation-entry/event-select/mobile-entry/operator-donations.
  # mobile-entry.ts itself (the phone-sized keypad form factor of this
  # same AC) fully wired 2026-09-08: event load by the `event` query
  # param, DonationService.createDonation, SyncEngineService-backed
  # pending count — same real data path as donation-entry.ts, just the
  # alternate ergonomic for roaming collectors.
  # Status is "review", not "done" — not yet manually verified in a
  # browser (no browser-automation tool available this session).
  3-1-record-a-donation-online-or-offline: review
  # 3-2/3-3/3-4 implemented 2026-09-07 as part of a broader "wire every
  # stubbed feature" effort (see plan at the time: operator-donations,
  # admin-donations, admin-trash all wired to real DonationDataService
  # methods, with a shared generalized writeAuditLog). Status "review" —
  # not yet manually browser-verified, and the live Appwrite `donations`
  # table may be missing the `updatedAt` column edits/deletes need to
  # actually sync (falls back to locally-saved-but-pending otherwise).
  3-2-donation-list-search-filter-running-total: review
  3-3-edit-a-donation-entry: review
  3-4-admin-soft-delete-a-donation: review
  # Implemented 2026-09-07: conflict detection (DonationDataService compares
  # baseUpdatedAt vs the row's current updatedAt before an update applies),
  # new recordConflict/resolveConflict Function actions (conflict-resolution.js),
  # donation_conflicts table + Function env var created via Appwrite MCP,
  # SyncEngineService (pendingCount/syncing, auto-drain on reconnect, manual
  # "Sync now"), admin-conflicts/ConflictResolver wired to real data.
  # Deployed via the Appwrite MCP server (discovered mid-deploy: the Function
  # is GitHub VCS-connected with providerBranches unrestricted, so a manual
  # MCP upload got stuck "waiting" — pushed this branch instead, VCS auto-
  # built it in 17s, then explicitly activated that deployment; smoke-tested
  # via a live execution — 401 Unauthenticated as expected, confirming the
  # new code is actually live and routing correctly).
  # Known gap: a retried (background-synced) update can't write a full audit
  # entry — previousValues/reason only exist at the original call site.
  # Status "review" — deployed and smoke-tested, but not manually browser-
  # verified end-to-end (no browser-automation tool this session).
  3-5-sync-engine-auto-sync-pending-count-conflict-detection: review
  # 3-6 implemented 2026-09-09: added a receipt-numbering scheme (AD-8) split across the client
  # and the Function. Client (src/app/data/models/receipt-numbering.ts): eventShortCode()
  # (type prefix + last 4 alnum chars of the event id) + provisionalReceiptNumber(), used by
  # DonationDataService.createDonation to assign a real `{shortCode}-P{n}` number up front
  # (replacing the old `P-${Date.now()...}` placeholder) — `n` is a device-local count of this
  # event's still-provisional receipts, so two offline donations on the same device never
  # collide, and a retired (already-synced) slot is safe to reuse. Server
  # (donation-recording.js): the Function is now the sole assigner of the canonical number —
  # atomically increments the Event's own `nextReceiptSeq` column (tablesDB.incrementRowColumn)
  # and writes `{eventShortCode}-{seq}` into the Donation row, ignoring the client-sent
  # provisional value for the actual write. DonationDataService.trySyncNow reads the Function's
  # response and overwrites the local record's receiptNumber with that canonical value —
  # nothing already printed needs reprinting, only the stored value changes, whether the sync
  # happens inline (online) or later via SyncEngineService's retry (was offline). The
  # eventShortCode logic is duplicated by hand between the TS model and the plain-JS Function
  # (no shared module system between the two deployments), same convention as shared.js's
  # VALID_ROLES/auth.service.ts note.
  # New ReceiptService (src/app/data/services/receipt.service.ts) generates the print-ready A5
  # PDF client-side via jsPDF (npm-installed per the architecture doc, not vendored like
  # SheetJS) — Event Name (prominent) + Type, a dividing line, a "PROVISIONAL" marker whenever
  # syncStatus isn't yet 'synced', Donor/Amount/Type/On-Behalf-Of/Date-Time/Receipt No/Operator
  # rows, and a thank-you message. downloadReceipt() saves as
  # `Receipt_[EventName]_[ReceiptNo].pdf`; printReceipt() opens the same PDF in a new tab with
  # autoPrint() set, no server round-trip either way. Wired into donation-entry.ts's 'saved'
  # phase, whose "Print receipt"/"Download PDF" buttons had been sitting unwired since Story 3.1.
  # Known gap (out of scope for this story): mobile-entry.ts has no post-save confirmation
  # screen at all (it resets immediately for the next donor, by design — see its own doc
  # comment), so this form factor has no receipt UI yet. The donation itself still gets a
  # correct receiptNumber either way (that path is shared), only the print/download affordance
  # is desk-only for now.
  # Deployed via the Appwrite MCP server same-day (same flow as Stories 2.2/2.4/3.5): pushed
  # this branch, VCS auto-built deployment 6aa1292302b989d312f6 in 22s, then explicitly
  # activated it. Smoke-tested via a live execution: an unauthenticated recordDonation call
  # returned 401 "Unauthenticated" with the execution's deploymentId matching the new build,
  # confirming it's live and routing through verifyCaller before ever reaching the new atomic
  # nextReceiptSeq increment.
  # Status "review" — 40 new/updated tests (17 Function, 23 Angular, all passing; 89/89 Function
  # suite, 206/207 Angular suite with the one failure being the pre-existing unrelated
  # app.spec.ts boilerplate test), `tsc --noEmit` clean, and the Function is deployed and
  # smoke-tested live. Not yet manually browser-verified end-to-end (no browser-automation tool
  # this session).
  # Follow-up fix 2026-09-09 (post-merge, found during manual browser verification): the
  # printed/downloaded receipt showed "GH µ 400.00" instead of "GH₵ 400.00" — jsPDF's standard
  # fonts (Helvetica) use WinAnsiEncoding, which doesn't cover the Ghana Cedi sign (U+20B5);
  # jsPDF doesn't reject the character, it silently mis-renders it (₵'s low byte happens to be
  # "µ" in WinAnsi). Fixed by vendoring DejaVu Sans (src/vendor/fonts/ — static TTF, confirmed
  # via `fc-query`'s charset output to cover the full Currency Symbols block before vendoring)
  # and registering it in ReceiptService via jsPDF's addFileToVFS/addFont, used specifically for
  # the receipt's Amount row. Verified beyond the mocked unit tests: generated a real (non-
  # mocked) PDF with the embedded font and confirmed via the raw PDF bytes that its ToUnicode
  # CMap correctly contains the 0x20b5 codepoint mapping.
  # Manually browser-verified 2026-09-09: PDF receipt generated and downloaded correctly,
  # including the Ghana Cedi sign fix above (confirmed rendering as "GH₵ 400.00", not the
  # earlier "GH µ" corruption). Status: done.
  3-6-receipt-generation-pdf-provisional-numbering: done
  epic-3-retrospective: optional

  epic-4: in-progress
  # 4-1/4-2 implemented 2026-09-07: first Appwrite Realtime use in the app
  # (DonationDataService.subscribeToChanges, admin-dashboard-only by
  # design), admin-dashboard stats/live-feed wired to real data. SheetJS
  # vendored per the AC (src/vendor/sheetjs/, never npm install xlsx),
  # ReportService.exportDonationsXlsx wired to admin-reports. "Awaiting
  # sync" stays a placeholder — architecturally can't be known from a
  # server-only read until Story 3.5 exists. Status "review" — not yet
  # manually browser-verified (no browser-automation tool this session).
  4-1-admin-real-time-dashboard: review
  4-2-admin-excel-export: review
  # 4-3/4-4 implemented 2026-09-08 alongside 2-4: family-live.ts now
  # reads real (sanitized) event+donation data via resolveAccessCode,
  # polling every 15s rather than a Realtime subscription — Family has
  # no Appwrite session to subscribe with (AD-10), stated as the
  # pragmatic v1, not a silent gap.
  # 4-3 manually browser-verified 2026-09-08: entered a live access code
  # at /family, landed on the live summary showing the real donation
  # (Solomon Quansah, GH₵200.00 cash, on behalf of Francisca) with no
  # donorPhone/recordedBy/notes present. Status: done.
  4-3-family-live-summary: done
  # 4-4's export button is wired to ReportService.exportDonationsXlsx(...,
  # { sanitized: true }) built in Phase 3 for exactly this reuse, but the
  # download itself hasn't been manually exercised yet — only 2-4/4-3's
  # generate/resolve path was. Status stays "review" until the .xlsx
  # download is actually checked in a browser.
  4-4-family-excel-export-sanitized: review
  # Implemented 2026-09-07: AuditLogDataService/AuditLogService (paginated,
  # admin-only read against audit_logs, no Dexie/offline path — a live
  # server view by design) wired into admin-audit.ts. Also fixed a gap the
  # AC itself surfaced: DonationDataService.createDonation now writes a
  # 'create' audit entry too (previously only edit/delete/recover did).
  # Status "review" — not yet manually browser-verified.
  4-5-audit-log-viewer: review
  epic-4-retrospective: optional

  epic-5: in-progress
  # Implemented 2026-09-09. Scoped via a code survey first (not a blind sweep) — found the
  # "final check, not first time" framing in this story's AC was optimistic: real gaps existed,
  # not just a sign-off pass.
  # Touch targets / iOS zoom (AC2/AC3): raised the shared .btn/app-input/select/radio/toggle/
  # checkbox components and several page-local controls (donation-form .input/.input--lg,
  # admin-donations .input/.filter-search, mobile-entry .type-seg, sidebar .collapse-toggle/
  # .nav-link/.logout-button, connection-banner .banner-action, pending-queue .queue-close,
  # filter-tags .filter-tag) to a 44px minimum; bumped every native text-input font-size found
  # under 16px (app-input, select, donation-form, admin-donations, mobile-entry .name-input) to
  # prevent iOS Safari's auto-zoom-on-focus.
  # Mobile nav (AC1): admin-layout/organizer-layout had NO responsive strategy at all — the
  # 240px sidebar just squeezed page content into an unusably narrow column below ~720px. Added
  # a hamburger toggle + off-canvas drawer to Sidebar (mobileOpen input/dismissMobile output,
  # accessible via a real (keydown.escape) handler per WCAG 2.1.1, same justified
  # eslint-disable-next-line pattern as the modal-scrim fix from the CI/lint cleanup) plus
  # wiring in both layouts. The desktop icon-only collapse feature is untouched/independent —
  # CSS resets it inside the mobile breakpoint since that trade-off doesn't apply to an overlay.
  # Table overflow (AC1): admin-users/admin-trash/admin-events all reuse admin-donations' table
  # markup but — being separate Angular components — never actually got its scoped .table-wrap/
  # .table CSS (including the overflow-x:auto the fix depends on). Promoted the whole block to
  # global styles.scss, single source of truth now.
  # Systematic audit (AC1): 27 component stylesheets had zero @media queries. Delegated a
  # thorough read of the .scss+.html pairs to background agents to separate genuine overflow/
  # touch-target risk from files that just happen to already be fluid; fixed every confirmed
  # finding: donation-form's .field-row (no min-width:0, native inputs' intrinsic width can't
  # shrink) and its .btn-save/.btn-clear (referenced but never defined anywhere — silently
  # unstyled, sub-20px buttons; switched to the real .btn/.btn-primary/.btn-secondary classes,
  # restoring what the file's own comment already claimed was true), admin-donations' .page-head
  # (no flex-wrap, title+total+actions row overflows ~344px vs ~312px available at 360px) and
  # its .field-row (same min-width:0 gap, and — since admin-events/admin-trash/admin-users/
  # admin-event-detail/edit-event all `@use` admin-donations.scss directly — this one fix
  # propagates to all five consuming screens for free), mobile-entry's .amount-value (fixed
  # 2.5rem risks overflowing 360px for a real 5-6-digit amount; switched to a clamp() that
  # shrinks instead of clipping an actual money value), checkbox/filter-tags (same missing
  # min-height:44px .form-row pattern as radio/toggle, plus filter-tags needed flex-wrap so a
  # long tag row doesn't force page-level horizontal overflow).
  # Explicitly NOT touched: `page-titles` component (zero remaining overflow issues found
  # elsewhere are worth noting this one is dead code — confirmed via grep, unreferenced by any
  # route or template, not one of the 26 real screens — fixing its responsive layout would be
  # wasted effort since no user can ever reach it).
  # Known limitation of this pass: no browser-automation tool was available this session, so
  # the AC's own actual verification methods — resizing a real viewport from 360px to 1920px
  # and checking for horizontal scrolling, and running an actual Lighthouse/AXE audit against
  # live rendered pages — could not be executed here. Everything above is a code-level fix
  # verified via `tsc`/`ng build`/`ng lint`/the full test suite (all green except the one
  # pre-existing unrelated app.spec.ts failure), not a substitute for that manual pass.
  # Status "review" — real, verified code fixes landed; this story cannot honestly move to
  # "done" until a human actually resizes the app across that width range on real screens
  # (including touch devices) and runs the AXE/Lighthouse sweep the AC calls for.
  5-1-responsive-layout-across-all-screens: review
  5-2-installable-pwa: backlog
  # Scoped 2026-09-12 via a code survey (not a blind implementation pass) against all 3 ACs:
  # AC1 (no data race, entries isolated by recording Operator's user ID) and AC3 (one Operator on
  # two devices, no conflict) were both already satisfied by prior stories, not new work:
  # - donation-recording.js's recordedBy is stamped server-side from the JWT-verified caller
  #   (verifyCaller/shared.js), never trusted from the client payload — concurrent Operators
  #   writing distinct donation rows are isolated by construction.
  # - The one genuinely shared resource, Event.nextReceiptSeq, already goes through Appwrite's
  #   native atomic incrementRowColumn (Story 3.6/AD-8), not app-level read-modify-write.
  # - auth.service.ts's login()/updateSession() both scope to `sessionId: 'current'` (this
  #   device's session only); idle-timeout state is localStorage-backed, inherently per-device.
  #   Appwrite's native multi-session-per-user model plus this app's per-device local state
  #   already gives two-devices-one-Operator with no conflict — nothing here assumes a single
  #   active session.
  # AC1's "tested under load" had a real gap though: every existing donation-recording.test.js
  # test calls the handler once — none actually ran calls concurrently. Added one
  # (functions/set-role-and-permissions/tests/donation-recording.test.js): 10 simulated Operators
  # call handleDonationRecordingRequest via Promise.all (not sequential awaits) against a shared
  # fake TablesDB (one events map, one donations map — modeling 10+ real Operators against the
  # same live event), asserting all 10 receipt numbers are unique and each donation's
  # recordedBy/donorName matches its own caller, not another concurrent one's. This proves the
  # handler carries no shared mutable state across concurrent invocations, which is what the
  # app-level code actually has to get right — real cross-request atomicity is Appwrite's
  # documented server-side guarantee, not something this test (or the handler) can substitute
  # for.
  # AC2 ("another Operator's entry... pushes it to my event list without a page refresh") was a
  # genuine gap, not a already-solved concern: Story 4.1's DonationDataService.subscribeToChanges
  # was wired admin-dashboard-only "by design", with a doc comment reasoning a live socket would
  # fight Operators' offline-first flow. That reasoning doesn't actually hold for
  # operator-donations.ts's "All desks" tab — the literal "event list" an Operator watches for
  # other desks' entries at the same live event — because every refetch already goes through
  # loadDonationsForEvent, which (via pullDonations) already skips any row with a pending outbox
  # entry. A live push therefore can never clobber a not-yet-synced local write. Wired
  # operator-donations.ts to the same subscribeToChanges primitive admin-dashboard already uses
  # (subscribe in ngOnInit after the initial load, refetch loadDonationsForEvent(eventId) on any
  # change, unsubscribe in ngOnDestroy) and corrected the now-stale "admin-dashboard-only by
  # design" doc comment on subscribeToChanges itself.
  # No new spec file for operator-donations.ts (it had none before this story) — matches this
  # codebase's existing depth of coverage for this exact pattern (admin-dashboard.spec.ts is
  # itself only a creation smoke test, not a behavioral test of its own Realtime wiring).
  # `tsc --noEmit`, `ng lint`, the full Angular suite (216/217, same one pre-existing unrelated
  # app.spec.ts failure as every prior story), and the full Function suite (90/90, including the
  # new concurrency test) all pass.
  # Status "review" — the code-level guarantees above are verified, but AC1's "tested under
  # load" and AC2's "no page refresh" ultimately describe a live, multi-device scenario (10+ real
  # devices hitting a live Appwrite project simultaneously) that no tool available this session
  # can actually exercise. Cannot honestly move to "done" until a human runs that live multi-
  # device test.
  5-3-multi-device-concurrent-operators: review
  epic-5-retrospective: optional

  # Epics 6-9 added 2026-09-25 (sprint planning): Organizer Multi-Tenancy & Role
  # Hierarchy capability, from prd-givio-2026-09-23 / architecture-givio-2026-07-25
  # (amended) / ux-givio-2026-09-23 / epics.md Epics 6-9. Story 6.1 was drafted then
  # retired during elicitation (route-rename plan reversed) -- Epic 6 starts at 6.2,
  # ID intentionally not reused.
  epic-6: in-progress
  6-2-tenant-membership-foundation: review
  6-3-credentials-per-relationship-enforcement: backlog
  6-4-organizer-onboarding-admin-invited-self-signup: backlog
  6-5-manual-verification-admin-approval-queue: backlog
  6-6-operator-event-switcher: backlog
  epic-6-retrospective: optional

  epic-7: backlog
  7-1-add-manage-team-members: backlog
  7-2-identity-check-on-team-additions: backlog
  7-3-revocation-preserves-attribution-for-cause-revocations-feed-the-bypass-list: backlog
  7-4-platform-wide-duplicate-event-detection: backlog
  7-5-tenant-scoped-audit-log: backlog
  epic-7-retrospective: optional

  epic-8: backlog
  8-1-admin-suspends-a-tenant: backlog
  8-2-admin-super-admin-access-logging: backlog
  8-3-contact-admin-support-form: backlog
  8-4-consolidated-cross-event-total: backlog
  8-5-periodic-settlement-export-report: backlog
  8-6-super-admin-manages-admin-accounts: backlog
  epic-8-retrospective: optional

  epic-9: backlog
  9-1-personal-device-prompt-auto-logout: backlog
  9-2-family-view-survives-organizer-suspension: backlog
  9-3-full-donation-visibility-for-family: backlog
  epic-9-retrospective: optional
